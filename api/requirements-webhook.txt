# Dependency riêng cho api/ (webhook nấc 1) -- KHÔNG gộp vào requirements.txt
# gốc ở đây, hợp nhất sau khi ráp xong (xem README.md "RÁP SAU").
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.0,<3.0
httpx>=0.27          # cần cho fastapi.testclient.TestClient (test_main.py)
