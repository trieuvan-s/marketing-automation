__pycache__/
*.py[cod]
.venv/
venv/
env/
.env
*.egg-info/
build/
dist/
.pytest_cache/
.DS_Store
.idea/
.vscode/

# Phase DATA-ROOT: dữ liệu runtime (documents/output/state/logs/ab) chuyển
# sang storage.data_root NGOÀI repo (mặc định ../marketing-automation-database,
# xem config/settings.yaml + src/twmkt/config.data_path()) — không còn thư mục
# storage/ trong repo nên KHÔNG cần rule gitignore riêng cho nó nữa.

# Bí mật — KHÔNG commit khóa service account / token
secrets/
*.json
# ...nhưng vẫn cho phép các file JSON cấu hình đã biết (không phải khóa)
!.vscode/*.json
!.claude/*.json
!tsconfig*.json
