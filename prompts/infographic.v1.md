Tạo spec infographic dạng JSON — TẤT ĐỊNH, $0.

Lưu ý: InfographicSpecAgent hiện KHÔNG gọi LLM (uses_llm=False, theo CLAUDE.md —
infographic ở Tầng 0/free). Số liệu (stats) trích THẲNG từ evidence bằng regex
nên không thể bịa. File này giữ để tài liệu hoá + đề phòng sau này nâng cấp agent
dùng LLM polish headline/insight (vẫn giữ số liệu tất định).
