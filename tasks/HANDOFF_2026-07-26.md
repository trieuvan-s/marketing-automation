# BÀN GIAO — phiên 2026-07-26 (agent-B, dừng GIỮA CHỪNG vì hết usage gói Plus)

> Đọc file này TRƯỚC khi làm gì tiếp. Đây là bàn giao GIỮA PHIÊN (không phải
> cuối 1 việc trọn vẹn) — agent tiếp theo đóng vai trò **agent-B**, tiếp tục
> ĐÚNG mạch việc đang dở, không phải bắt đầu lại. Có 2 việc ĐỘC LẬP đang chạy
> song song: (1) Phần D video polish + disclaimer một-nguồn-sự-thật — ĐÃ XONG,
> chờ Lead duyệt merge; (2) điều tra bug "source rỗng" trên infographic — CHƯA
> XONG, đang treo chờ bằng chứng thật từ agent-A (Bước 5.3).

## TÌNH TRẠNG GIT NGAY LÚC DỪNG

### `marketing-automation` (repo này)
```
develop                                  801a6ef  (= origin/develop, sạch)
feature/infographic-frame                09ab1d9  ĐÃ MERGE vào develop từ trước — lịch sử, bỏ qua
fix/infographic-disclaimer-single-source e1c9e15  CHƯA merge — đã push origin (an toàn), CHỜ Lead duyệt
main                                     29d68bf  (không đụng)
```
Working tree: sạch, đang đứng ở `develop`.

### `aigen` (`D:\trung-temp\aigen`)
```
develop                                   cc473a5  (= origin/develop, sạch)
feature/video-polish-music-xfade-kenburns 9c7e9bb  CHƯA merge — đã push origin (an toàn), CHỜ Lead duyệt
```
Working tree: sạch, đang đứng ở `feature/video-polish-music-xfade-kenburns`.

**Cả 2 nhánh việc dở đã được push lên origin trong lượt bàn giao này** (trước đó
chỉ nằm local) — phòng trường hợp phiên sau đổi máy.

---

## VIỆC 1 (ĐÃ XONG, CHỜ DUYỆT) — Phần D video polish + disclaimer một nguồn

### `aigen` — nhánh `feature/video-polish-music-xfade-kenburns` (5 commit)

| Commit | Nội dung |
|---|---|
| `81e050e` | Ken Burns pan/zoom cho nền `frame-liquid-bg-hero` (9:16) — bọc riêng lớp blob+particle, không đụng `.scrim`, zoom 1.0→1.16 trong 5s rồi giữ nguyên. |
| `fac5ae7` | Xfade thay hard-cut trong `concatVideos()` — duration = đúng `SCENE_GAP_SEC` (0.3s) để không lệch đồng bộ audio. Đã xác minh bằng toán + clip màu tổng hợp thật. |
| `355f028` | Nhạc nền CC-BY 4.0 ("Inspired" — Kevin MacLeod, incompetech.com) trộn dưới giọng đọc. File `.mp3` git-ignored (`MUSIC/*/*.mp3`), attribution ghi trong `MUSIC/NOTICE.md` (tracked). |
| `10de51c` | Nhạc nền ra sau cờ `config/render.config.json::music.enabled` (**mặc định `false`** — quyết định Lead: thử nghiệm, sản phẩm chưa public). Bật cờ mà thiếu `attribution` → **throw ngay**, đã xác nhận qua CLI thật. |
| `9c7e9bb` | Disclaimer: `ContentOutputVideo.disclaimer` (đã có sẵn, KHÔNG cần thêm field) — sửa hành vi từ "im lặng bỏ qua nếu rỗng" thành **throw** nếu cả top-level lẫn scene-level đều rỗng. |

Test: typecheck sạch, **243/243 pass**. Đã render thật 1 video PNJ end-to-end
(script tại `marketing-database/aigen-pipeline/output/pnj-quy-ngoai-ban-rong-20260724/`),
xem từng khung hình xác nhận Ken Burns/xfade/nhạc/disclaimer đều đúng.

**Việc còn lại**: Lead xác nhận merge `feature/video-polish-music-xfade-kenburns` → `develop` (aigen)
rồi push. KHÔNG tự merge — quy tắc "không agent nào commit thẳng vào develop"
chỉ được nới 1 lần trước đó, không phải quyền thường trực.

### `marketing-automation` — nhánh `fix/infographic-disclaimer-single-source` (1 commit)

`e1c9e15` — `ai_full.py::render_ai_full()` bỏ hẳn override
`settings.get("render.infographic.disclaimer", ...)` (2 nguồn brand.yaml/settings.yaml
từng lệch nhau đúng 1 dấu chấm cuối) — disclaimer giờ đọc DUY NHẤT từ
`config/brand.yaml::footer.disclaimer`. Gỡ khoá chết trong `settings.yaml`.
`brand_stamp.py` không cần sửa (đã là tham số thuần từ trước).

Test: 539/539 pass (lúc đó). **Việc còn lại**: Lead xác nhận merge vào `develop`
(marketing-automation) — cân nhắc conflict với `f10dee4`/`801a6ef` (agent-A vừa
sửa disclaimer ở `production.py` — KHÁC file, khác lớp, nhưng nên rebase lại
trước khi merge cho chắc).

---

## VIỆC 2 (ĐANG DỞ, TREO CHỜ BẰNG CHỨNG) — bug "source rỗng" trên band đáy infographic

**ĐỌC KỸ MỤC NÀY TRƯỚC KHI ĐỘNG VÀO BẤT KỲ FILE NÀO LIÊN QUAN SOURCE.**
Đây là phần dễ đi lạc nhất — 3 lượt chẩn đoán trước đó ĐỀU sai, mỗi lượt "nghe
rất thuyết phục" nhưng chỉ đúng khi có bằng chứng CHẠY THẬT xác nhận.

### Bối cảnh bug quan sát được
Ảnh infographic thật (Techcombank, PNJ — 2 dòng CONTENT test dùng xuyên suốt
Phần A-D) có band đáy trống dòng "Nguồn:" (`source_text` rỗng trong log JSON).
Lead ban đầu nghi: `ProductionSpec.source_url` (Python dataclass,
`media_factory/spec.py`) là nguyên nhân — infographic "đi qua" field tên khác
(`source_url`) nên giá trị `source` bị rơi.

### Đã điều tra, đã LOẠI TRỪ (không cần lặp lại)
1. ~~"Thiếu dữ liệu nguồn ở Sheet"~~ — SAI. Gọi thật
   `board.read_approved_context()`: cả Techcombank lẫn PNJ đều có URL hợp lệ
   (`https://cafef.vn/...`) trên cột CONTEXT.Source.
2. ~~"Lệch tên trên đường sản xuất thật (production.py dùng field khác
   ProductionSpec)"~~ — SAI theo nghĩa Lead nêu ban đầu. **`ProductionSpec`
   (Python dataclass, `media_factory/spec.py`) KHÔNG hề được khởi tạo ở BẤT KỲ
   ĐÂU trong code thật** — `grep "ProductionSpec("` toàn repo Python chỉ khớp
   `tests/test_pipeline.py`. `production.py` xây `CONTENT.Output` bằng **dict
   Python xây tay trực tiếp** (`render_video()`, `infographic_spec_from_data()`
   v.v.), KHÔNG đi qua dataclass này. Dòng `"source": domain_of(brief.url)`
   (đúng, dùng `brief.url` lấy từ Sheet CONTEXT.Source) đã có từ **commit
   `0bde337c`, 2026-07-10** (git blame xác nhận) — TRƯỚC CẢ khi 2 bài
   Techcombank/PNJ (sự kiện 21-22/7) tồn tại.
3. `ProductionScene`/`verify_spec()` (cùng file `spec.py`) — CŨNG chỉ được
   `tests/test_pipeline.py` gọi, không caller thật nào khác. Guardrail-2 ẢNH
   (verify_spec cho `ProductionBlock`) đã bị **XOÁ TƯỜNG MINH** 2026-07-21
   (docstring `render_production_assets.py`) — quyết định Lead, không phải sơ
   suất. Guardrail-2 VIDEO thật hiện sống bên `aigen` (TypeScript,
   `production-spec/guardrail/verify-spec.ts`), KHÔNG phải bản Python này.

   ⚠️ **PHÂN BIỆT QUAN TRỌNG** (đã gây hiểu lầm 1 lượt, xem hội thoại): có
   **HAI thứ tên gần giống nhau, ở HAI repo khác nhau**:
   - `aigen/src/production-spec/` (TypeScript) = **CÂY CẦU THẬT, đang chạy**
     (`ContentOutputVideo` → `TemplateScript`, 243 test, đã sửa/test thật
     phiên này ở Việc 1 trên). **GIỮ, KHÔNG đụng.**
   - `marketing-automation/src/twmkt/media_factory/spec.py::ProductionSpec`
     (Python dataclass) = **BẢN NHÁP CŨ, CHẾT**. Đây là thứ đang xét dọn.

### Giả thuyết hiện tại (CHƯA XÁC NHẬN bằng chạy thật)
2 dòng CONTENT Techcombank/PNJ nhiều khả năng là **dữ liệu soạn/sửa TAY** ở
Gate 2 (dùng làm test render ảnh xuyên suốt Phần A-D), KHÔNG sinh ra từ một
lượt chạy thật của `InfographicSpecAgent` — nên thiếu `source` là thuộc tính
của dữ liệu test tay, không phải bug đang sống trong pipeline. **Đây MỚI LÀ
GIẢ THUYẾT, chưa phải kết luận** — 3 lượt trước mỗi lượt đều tưởng chắc rồi
lại sai.

### ĐANG CHỜ — Bước 5.3 của agent-A
agent-A sẽ chạy: reset store + crawl mới + 1 bài end-to-end qua khâu render
infographic THẬT (không phải dữ liệu tay). Kết quả ảnh render:
- **"Nguồn: CafeF" hiện ra đúng** → xác nhận giả thuyết trên ĐÚNG. Bug tự hết
  do dữ liệu sạch, KHÔNG phải do sửa code. Việc còn lại CHỈ là dọn dead code
  (xem Việc 2.2 dưới) — ⚠️ ghi rõ trong báo cáo là "dọn dead code", KHÔNG được
  viết "đã sửa bug source" (2 việc khác nhau, không nhân quả).
- **Vẫn rỗng** → có bug thật trên đường `CONTENT.Output`, cần truy với dữ liệu
  thật trong tay (log tạm tại đúng chỗ dựng field `source`, tìm ra rồi sửa,
  gỡ log tạm trước khi commit).

**KHÔNG tự chạy Bước 5.3** — đây là vùng `produce_from_sheet.py`/`store/`/
`sheets_board.py`, agent-A đang động vào, KHÔNG được đụng.

### Việc 2 (đã làm xong lượt trước, chỉ đọc, CHƯA xoá gì) — bản đồ dọn dead code

Đối chiếu toàn bộ field `ProductionSpec`/`ProductionScene` (spec.py) với code
thật — bảng đầy đủ:

**`ProductionSpec`** (8 field khai): `title`, `facts` — KHỚP thật. `scenes` —
khớp tên ngoài, lệch cấu trúc trong. `source_url` — **DEAD + LỆCH TÊN + ĐA
NGHĨA** (thật dùng `"source"` giữ domain, không phải URL đầy đủ như tên gợi
ý). `topic_key`, `channel`, `aspect`, `variant` — DEAD hoàn toàn, không nơi
nào khác dùng. **Thiếu hẳn khỏi schema**: `schema_version`, `disclaimer` (dict
thật có, `ProductionSpec` không khai).

**`ProductionScene`** (5 field): `role`, `visual_kind` — KHỚP. `slots` — LỆCH
(thật dùng `"payload"`). `voice_text` — LỆCH (thật dùng `"narration"`).
`fact_ref` — tên khớp nhưng Python không bao giờ điền giá trị.

→ **Tổng: 3 field LỆCH TÊN/ĐA NGHĨA, 4 field DEAD hoàn toàn** — vượt ngưỡng
"2-3 field" Lead đặt ra, tức là **DỪNG KHI #1 đã kích hoạt cho Việc 2** ở lượt
trước — đã báo Lead, đang chờ hướng (gộp chung với chờ 5.3, vì quyết định dọn
hay không phụ thuộc kết quả đó).

**17 test trong `tests/test_pipeline.py:8210-8508`** chỉ test thuật toán
`verify_spec()` thuần (dead code), KHÔNG test nào chạm `production.py` thật
hay `CONTENT.Output` — nếu xoá dataclass, cả 17 test này nên **BỎ HẲN** (không
có cách "chuyển" hợp lý vì chúng không test hàm nào còn sống). Danh sách đầy đủ
từng test + lý do: xem lại phần hội thoại "Việc 1 (Nhánh 1)" ngay trước bàn
giao này (giữ nguyên trong lịch sử chat, không lặp lại ở đây cho gọn).

⚠️ **2 test LÂN CẬN, KHÔNG được xoá nhầm**: `test_visual_kind_closed_set_and_
deferred_avatar` (dòng 8458, test `VISUAL_KINDS`/`DEFERRED_VISUAL_KINDS` —
THẬT, `production.py` import sống) và `test_matches_canonical_fact_range_and_
delta_shapes` (dòng 8513, test `production.py::unsupported_numbers()` — THẬT,
guardrail lần 1 đang chạy).

### Việc kế tiếp theo đúng thứ tự (khi có kết quả 5.3)

1. Lead báo kết quả ảnh render Bước 5.3.
2. **Nếu "Nguồn: CafeF" đúng**: xoá `ProductionSpec`/`ProductionScene`/
   `verify_spec`/`_fact_ref_for_texts`/`_check_plain_list_item_entity` khỏi
   `media_factory/spec.py` (giữ `VISUAL_KINDS`/`DEFERRED_VISUAL_KINDS`/
   `Violation`... — kiểm lại cái nào thật sự chỉ phục vụ phần dead trước khi
   xoá). Xoá 17 test tương ứng, KHÔNG comment-out. `grep "ProductionSpec"
   "source_url"` phải sạch (trừ `aigen/production-spec/` TS, khác hẳn, giữ
   nguyên; trừ `sheets_board.py::context_row(source_url=...)` — tham số hàm
   helper THẬT khác nghĩa, KHÔNG đụng, xem "Không đụng" dưới).
3. **Nếu vẫn rỗng**: dò `CONTENT.Output` dict thật với dữ liệu thật Bước 5.3
   vừa tạo, tìm đúng chỗ `source` mất giá trị, sửa, gỡ log tạm.
4. Nghiệm thu cả 2 nhánh: render lại 1 infographic thật, xác nhận "Nguồn:
   CafeF" (hoặc domain đúng) hiện trên band đáy. Thêm test: dựng CONTENT.Output
   từ document có `"source"="CafeF"` → output phải có source đúng tên field
   + đúng giá trị (không rút domain nữa nếu Lead chốt hiển thị source_name).
5. Merge Việc 1 (Phần D + disclaimer) — ĐỘC LẬP với Việc 2, có thể làm trước.

---

## RANH GIỚI (giữ nguyên cho phiên sau)

**Được sửa** (Việc 2, sau khi có 5.3): `media_factory/spec.py`, `production.py`
(phần infographic + CONTENT.Output), `infographic.py`, `ai_full.py`,
`tests/test_pipeline.py`.

**KHÔNG đụng**: `store/`, `sheets_board.py`, `produce_from_sheet.py` (agent-A
đang động vào — Bước 3/5). `collectors/`, `curation/`, `validators/`.
`aigen/src/production-spec/` (TypeScript — cây cầu thật, đang chạy, GIỮ).
`"sources"` số nhiều ở `AnalysisWriterAgent` (khác nghĩa với `"source"` số ít).
`context_row(source_url=...)` trong `sheets_board.py` (tham số hàm helper
thật, khác hoàn toàn `ProductionSpec.source_url` dù trùng chữ).

**Nguyên tắc đặt tên đã chốt cho toàn dự án** (áp mọi thay đổi sau này): tên
biến ĐƠN NGHĨA — 1 biến 1 nghĩa, cấm tên ghép đa nghĩa (như `source_url` từng
mang 3 nghĩa: tên nguồn/URL đầy đủ/domain). DB có sẵn `"source"`="CafeF" và
`"url"`=địa chỉ đầy đủ — đọc thẳng trường, không tạo giá trị phái sinh khi dữ
liệu gốc đã có sẵn (vd không rút domain nếu hiển thị đã có tên nguồn thật).

**Rebase lên `develop` mới nhất trước khi làm bất cứ gì** — agent-A merge liên
tục.

---

## NGUYÊN TẮC RÚT RA PHIÊN NÀY (đừng lặp lại)

- **"Chắc chắn" không phải bằng chứng.** 3 lượt chẩn đoán liên tiếp đều "nghe
  rất thuyết phục" (thiếu dữ liệu → lệch tên trên đường thật → dead code do
  test tay) và 2 lượt đầu SAI khi đối chiếu với dữ liệu thật (gọi
  `read_approved_context()` trực tiếp, `git blame`, `grep` toàn repo). Chỉ
  lượt thứ 3 (dead code) có bằng chứng ĐỌC vững, nhưng **vẫn chưa phải bằng
  chứng CHẠY** — đó là lý do phải chờ Bước 5.3 thay vì tự tin sửa ngay.
- **Tên trùng chữ, khác nghĩa, khác repo → luôn hỏi "cái nào?" trước khi kết
  luận.** "ProductionSpec" (Python dead) vs "production-spec" (TypeScript,
  cây cầu thật) suýt gây hiểu lầm nghiêm trọng — Lead đã phải dừng lại hỏi
  "bạn có thực sự hiểu kiến trúc?". Luôn nêu rõ đường dẫn file đầy đủ khi nói
  về 1 class/module có khả năng trùng tên.
- **Đọc code không thay được chạy code khi kết luận có ảnh hưởng tới sửa dữ
  liệu/schema thật.** Đã đọc rất kỹ (git blame, grep toàn repo, gọi hàm đọc
  Sheet thật) nhưng vẫn giữ nguyên tắc: KHÔNG sửa `source` cho tới khi có 1
  lượt chạy thật (Bước 5.3) xác nhận.
- Dọn dead code và sửa bug là HAI việc, HAI lý do báo cáo khác nhau — đừng
  gộp thành "đã sửa bug" nếu bug tự hết do dữ liệu sạch chứ không do code đổi.

---

## GHI CHÚ VẬN HÀNH

- OmniVoice TTS server (aigen, port 8899) đã được khởi động trong phiên này để
  render video PNJ thật — **KHÔNG chắc còn sống** khi phiên mới bắt đầu (tiến
  trình nền không sống sót qua phiên, theo đúng CLAUDE.md của `aigen`: "phải
  restart mỗi phiên mới"). Muốn render video thật lại thì khởi động lại:
  ```
  PYTHONIOENCODING=utf-8 .venv-omnivoice/Scripts/python.exe scripts/omnivoice_server.py --port 8899 --device cuda:0 --dtype float16
  ```
- Không có việc gì đang chạy nền (background task) lúc dừng phiên.
- `TaskList` nội bộ của phiên này (Việc 8-17, Phần D + Việc 2 audit) đã hoàn
  tất mục tiêu của chúng — phiên sau nên tạo task list MỚI theo đúng thứ tự
  "Việc kế tiếp" ở trên, không cần khôi phục task cũ.
