# ACTIVE_TASK.md — Task đang làm (template rỗng)

> Điền lại mỗi khi bắt đầu 1 task mới có quy mô đáng kể (nhiều phase). Task
> nhỏ/1-lệnh không cần dùng file này. Sau khi task xong, chép tóm tắt sang
> `docs/HANDOFF.md` rồi XOÁ nội dung ở đây, trả về khung rỗng cho task kế tiếp.

## Mục tiêu
<1-2 câu: đích cuối của task, vì sao làm bây giờ>

## Scope — CHỈ được đụng
<liệt kê rõ file/thư mục được phép sửa>

**TUYỆT ĐỐI KHÔNG đụng:** <liệt kê rõ file/thư mục cấm — nếu phát hiện cần sửa
ngoài phạm vi này → DỪNG-BÁO-CÁO, không tự làm>

## Giới hạn context
<vd: không chạy test nếu không đổi code; không gọi LLM/ghi Sheet nếu là task
tài liệu; discovery chỉ 1 lần nếu việc lặp lại tốn token>

## Phase + Dừng-báo-cáo
### Phase 0 — <tên>
<việc cần làm>

**DỪNG. BÁO CÁO. Chờ duyệt.**

### Phase 1 — <tên>
<việc cần làm>

**DỪNG. BÁO CÁO. Chờ duyệt.**

<thêm phase khi cần — mỗi phase đều có dòng DỪNG-BÁO-CÁO riêng>

## Nghiệm thu
<danh sách gạch đầu dòng: điều kiện ĐÓNG task — cụ thể, đo được, không mơ hồ>
