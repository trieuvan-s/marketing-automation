# Phụ thuộc lõi (config-first). Cài: pip install -r requirements.txt
pyyaml>=6.0          # đọc config/settings.yaml

# Cổng duyệt qua Google Sheets (approval.sheets_gate) — chỉ cần khi gates.*.type = sheets
gspread>=6.0
google-auth>=2.0

# LLM thật cho Researcher + Hook (llm.provider = anthropic). Cần ANTHROPIC_API_KEY.
anthropic>=0.40

# Nạp ANTHROPIC_API_KEY bền từ secrets/.env (gitignored) — khỏi set env mỗi phiên.
python-dotenv>=1.0

# Tùy chọn khi lên production (xem pyproject optional-dependencies):
#   crawl4ai>=0.4      # Crawl4aiCollector (fallback nguồn cần JS)
#   langgraph>=0.2     # orchestrator production

# Bước 2 — HTTP-first collector (thuần Python, chạy cả trên 3.14)
httpx>=0.27
beautifulsoup4>=4.12

# Infographic Hybrid — render_mode="ai_full" (MẶC ĐỊNH) đóng dấu brand LÊN
# TRÊN ảnh AI qua render/brand_stamp.py (Pillow compositing). PHÁT HIỆN
# 2026-07-22 (agent-B, lúc rebase feature/infographic-hybrid): brand_stamp.py
# đã import PIL nhưng thiếu khai báo Ở ĐÂY — thêm cho khớp code thật.
pillow>=10.0
